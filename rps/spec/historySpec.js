describe('history', () => {
    describe('when no rounds have been played', () => {
        it('tells the ui that no rounds have been played', () => {
            // 準備
            // 実行
            // 期待
        })
    })
})
